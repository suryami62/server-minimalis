# ==============================================================================
#                      NGINX PROXY MANAGER - ENVIRONMENT VARIABLES
# ==============================================================================
#
# This file serves as a template for the required environment variables.
#
# ------------------------------------------------------------------------------
# INSTRUCTIONS:
# 1. Rename this file from `.env.example` to `.env`.
# 2. Replace the placeholder values with your actual credentials and settings.
# 3. Ensure that the `.env` file is included in your `.gitignore` to prevent
#    sensitive data from being committed to version control.
# ------------------------------------------------------------------------------

# --- PostgreSQL Database Credentials ---
#
# The following credentials are used to establish a connection to the
# PostgreSQL database. These values are critical for the initial setup and
# ongoing operation of Nginx Proxy Manager.
#
# IMPORTANT:
# - Replace the default values with strong, unique credentials.
# - These variables are used by the `docker-compose.yml` file to configure
#   both the Nginx Proxy Manager and the database service.
#

# Specifies the name of the PostgreSQL database to be created and used.
# Default: npm
POSTGRES_DB=npm

# Defines the username for accessing the PostgreSQL database.
# This user will be granted full privileges on the specified database.
# Default: npm
POSTGRES_USER=npm

# Sets the password for the PostgreSQL user.
# It is crucial to replace this with a very strong and secure password.
# A strong password should be long and contain a mix of uppercase letters,
# lowercase letters, numbers, and special characters.
# Default: replace_with_a_very_strong_password
POSTGRES_PASSWORD=replace_with_a_very_strong_password
